# 후쿠오카 · 유후인 숙소 최저가 모니터

2026년 8월 20~24일 일본 여행의 숙소 후보 6곳을 **1시간마다 자동 조회**해서
최저가를 기록하고, 일행이 볼 수 있는 웹페이지로 보여준다.

## 조회 조건

| 항목 | 값 |
|---|---|
| 인원 | 성인 3명, **1객실** |
| 후쿠오카 | 2026-08-20 → 08-22 (2박) |
| 유후인 | 2026-08-22 → 08-24 (2박) |

인원을 3명으로 지정해 조회하므로 **3명을 수용할 수 없는 2인실은 응답에 아예 포함되지 않는다.**
따로 걸러낼 필요가 없다.

## 대상 숙소

| 도시 | 숙소 | 네이버 ID | 라쿠텐 번호 |
|---|---|---|---|
| 후쿠오카 | 더 베이직스 후쿠오카 | N5041269 | 177662 |
| 후쿠오카 | 더 블러썸 하카타 프리미어 | N4849822 | 172876 |
| 후쿠오카 | 미쓰이 가든 호텔 후쿠오카 나카스 | N5007190 | 178294 |
| 후쿠오카 | 크로스 라이프 하카타 텐진 | N5303716 | 184507 |
| 유후인 | 야마노호텔 무소우엔 | KYK1070636135 | 44815 |
| 유후인 | 바이엔 | N1736235 | 39494 |

숙소 식별은 네이버 링크 → 실제 예약 페이지 → 라쿠텐 시설 페이지(시설명·주소 대조) 순으로 교차검증했다.

## 데이터 소스

네 곳을 쓴다. 하나만으로는 최저가를 놓친다. 실제로 경로마다 최저가가 다르다.

| 경로 | 붙인 이유 | 실측 |
|---|---|---|
| 네이버 | OTA 10곳을 한 번에 | 트립닷컴 직접값과 0.05% 일치 |
| 아고다 직접 | 네이버가 아고다 일부 채널을 빠뜨림 | 블러썸에서 92,983원 더 쌈 |
| 공식 홈페이지 | 어느 집계 사이트에도 없음 | 크로스라이프 -45,733원, 블러썸 -43,003원 |
| 라쿠텐 | 료칸 1박2식은 해외 OTA에 없음 | 무소우엔 최저가 |

### 1. 네이버 호텔 (`hermes-hotel-svc-api.naver.com/graphql`)

아고다·부킹닷컴·트립닷컴·호텔스닷컴·여기어때·하나투어·모두투어 등
**국내외 판매처 요금을 한 번에** 돌려준다. 원화 기준이고 엔화도 같이 온다.

- 인증·API 키·쿠키 전부 불필요
- `adultCnt: 3` 으로 3인 수용 객실만 필터링
- `userRate` 는 **체류 전체 총액**(1박 아님)
- `localRate` 는 판매처에 따라 `null` 일 수 있음 → 원화를 기준값으로 씀

> 응답의 `totalCount` 는 호출마다 흔들린다(네이버가 각 OTA에 실시간 질의를 뿌리고
> 타임아웃 안에 응답한 것만 모으는 구조로 보인다). 최저가 자체는 안정적이라
> 시계열 추적 지표로 **최저가만** 쓴다.

### 2. 라쿠텐 트래블 (`hotel.travel.rakuten.co.jp/hotelinfo/plan/`)

일본 내수 재고. **유후인 료칸의 1박2식(석식+조식) 플랜은 해외 OTA에 잘 안 올라와서**
네이버만 보면 통째로 놓친다. 실제로 무소우엔은 라쿠텐이 더 쌌다.

- 서버사이드 렌더링이라 JS 실행 없이 파싱됨 (WAF 없음)
- 요금은 `hinfo.hotels` 인라인 JSON 의 `sumTotalChargeTaxInclusive` (세금포함 총액)
- 객실명은 `<li id='{플랜ID}-{객실코드}' class='rm-type-wrapper'>` 의 `title` 속성
- **식사·정원은 같은 블록의 `data-locate="roomType-option-meal"` / `-people` 에서 읽는다.**
  플랜명 문구(`素泊まり`, `会席` …)로 추측하면 틀린다 — 무소우엔의 `期間限定特別特価プラン` 은
  이름에 식사 언급이 없는데 실제로는 1박2식이다. 료칸은 식사 유무가 금액의 대부분이라
  여기서 틀리면 비교가 무의미해진다.
- 엔화만 주므로 네이버 응답에서 역산한 환율로 원화 환산 (`krw_estimated: true` 로 표시)

환율은 별도 API를 쓰지 않는다. 네이버가 **같은 상품의 원화와 엔화를 동시에** 주므로
그 비율의 중앙값을 쓴다.

## 구조

```
collector/
  config.py          일정·호텔 정의 (여기만 고치면 대상 변경됨)
  collect.py         수집 → docs/data 산출
  sources/naver.py   네이버 GraphQL
  sources/rakuten.py 라쿠텐 HTML 파서
docs/                GitHub Pages 루트
  index.html app.js style.css
  data/latest.json   현재 스냅샷 (페이지가 읽음)
  data/history.jsonl 호텔별 최저가 시계열 (append only)
.github/workflows/collect.yml   매시 정각 크론
```

의존성 없음. 파이썬 표준 라이브러리만 쓴다.

## 로컬 실행

```bash
python3 collector/collect.py          # 수집
python3 -m http.server 8000 -d docs   # http://localhost:8000
```

## 자동화

`.github/workflows/collect.yml` 이 매시 정각(UTC)에 돌면서 수집 → `docs/data` 커밋을 한다.

Pages 는 **main 브랜치의 `/docs` 를 그대로 서빙**하도록 설정돼 있어서
커밋이 곧 배포다. 별도 배포 잡이 없다.

공개 주소: <https://immortalemployee2021-gif.github.io/japan-hotel-watch/>

> GitHub 크론은 **정각에 정확히 돌지 않는다.** 혼잡하면 수 분~수십 분 밀린다.
> "1시간마다"는 정확한 주기가 아니라 평균 주기로 보면 된다.

수동 실행은 Actions 탭 → `숙소 최저가 수집` → Run workflow.

## 페이지를 고칠 때

`docs/index.html` 의 `style.css?v=N` / `app.js?v=N` 숫자를 **반드시 올린다.**
안 올리면 GitHub Pages 캐시 때문에 일행이 한동안 옛 화면을 본다.
`docs/data/*` 는 `app.js` 가 분 단위 쿼리스트링을 붙여 읽으므로 신경 안 써도 된다.

## 알아둘 것

- 표시 금액에 **결제 단계의 수수료·숙박세가 빠져 있을 수 있다.** 최종 금액은 예약 화면에서 확인.
- 라쿠텐 원화 금액은 환산값이라 카드 실제 청구액과 다르다.
- 료칸은 **소박(식사 없음)과 1박2식의 금액 차이가 크다.** 페이지의 식사 배지를 보고 비교할 것.
- 3인 투숙 시 세 번째 잠자리가 엑스트라 베드/소파베드/이불 중 무엇인지는 숙소마다 다르다.
